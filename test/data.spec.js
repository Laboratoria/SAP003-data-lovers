require("../src/data.js");

describe("filterDatas", () => {
  it("is a function", () => {
    expect(typeof filterDatas).toBe("function");
  });

  //it("returns `example`", () => {
  //  expect(example()).toBe("example");
  //});
});
